
<!-- 
	add to the form a loop through the atrributes list, 
	display all optional atrributes to the park.
	each one will be connected with a check box.
	if the checkbox is selected/true it will need to be added to the proper attribute in the database with the form submissiom.

	<div id="attributes">
							<p> this is where the atrributes go</p>
							<%for(let x in attributes) {%>
							<div class="form-check form-switch">
								<input class="form-check-input" type="checkbox"  name="skateground[attributes]" id="attributes" default>
								<label class="form-check-label" for="attributes"><%=attributes[x] %></label>
							</div>
							<%} %>
					</div>  
-->
